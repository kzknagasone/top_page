# top page test

top page test source code.

## Configuration

Set the environment variables in `.env`.

- **Contact**: kzk.nagasone0314@magic.ocn.ne.jp

## Setup

1. Clone repository
2. Copy `.env.example` to `.env`
3. Run `npm install`
